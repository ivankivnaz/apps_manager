<%= form_with(model: web_application, local: true) do |form| %>
  <% if web_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_application.errors.count, "error") %> prohibited this web_application from being saved:</h2>

      <ul>
      <% web_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :path %>
    <%= form.text_field :path %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
